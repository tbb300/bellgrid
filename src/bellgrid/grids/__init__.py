"""bellgrid grids: state and action discretization backends.

Concrete classes (RegularGrid, WarpedGrid) live in sibling modules and will
be re-exported here once implementations land. See docs/api.md for the
intended public surface.
"""
